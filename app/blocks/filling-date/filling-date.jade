mixin filling-date(data)
	- data = data || {}
	+b.filling-date&attributes(attributes)
		+e('input').input(
			type='text'
			name=data.id
			id=data.id
			value=data.value
		)
		+e('label').label(for=data.id)=data.label
